# Web app dependencies. The ML versions are pinned to the ones the saved
# models were verified to unpickle under -- changing them risks breaking
# joblib.load() on the .pkl files.
Django>=5.0
PyMySQL>=1.1
pandas>=2.0
numpy>=1.26
scikit-learn==1.9.0
xgboost==3.3.0
joblib>=1.3
pyarrow>=14

# Serving
gunicorn>=21.2
whitenoise>=6.6
