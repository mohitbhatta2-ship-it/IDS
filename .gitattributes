*.pkl filter=lfs diff=lfs merge=lfs -text
*.parquet filter=lfs diff=lfs merge=lfs -text

# Experimental candidate models: store as normal blobs (LFS push is not available here)
validation/models/realistic_pcap_candidate/model.pkl !filter !diff !merge
validation/models/realistic_pcap_candidate_v2/*.pkl !filter !diff !merge

# Targeted-benign candidate models: store as normal blobs (LFS push unavailable here)
validation/models/targeted-benign-candidate/*.pkl !filter !diff !merge

# Balanced real-PCAP candidate models: store as normal blobs (LFS push unavailable here)
validation/models/balanced-real-candidate/*.pkl !filter !diff !merge

# FTP-behavioural candidate models: store as normal blobs (LFS push unavailable here)
validation/models/ftp-behavioral-candidate/*.pkl !filter !diff !merge

# Robustness ablation models: store as normal blobs (LFS push unavailable here)
validation/models/ftp-behavioral-robustness/*.pkl !filter !diff !merge

# Robust-retraining candidate + ablation models: store as normal blobs (LFS push unavailable here)
validation/models/ftp-behavioral-robust-retraining/*.pkl !filter !diff !merge

# Extended-behavioural candidate models: store as normal blobs (LFS push unavailable here)
validation/models/ftp-behavioral-ext/*.pkl !filter !diff !merge

# ftp_failed_logins ablation candidate models: store as normal blobs (LFS push unavailable here)
validation/models/ftp-failed-login-ablation/*.pkl !filter !diff !merge

# Cross-session candidate models: store as normal blobs (LFS push unavailable here)
validation/models/ftp-cross-session/*.pkl !filter !diff !merge

# Per-connection candidate + ablation models: store as normal blobs (LFS push unavailable here)
validation/models/ftp-per-connection/*.pkl !filter !diff !merge
